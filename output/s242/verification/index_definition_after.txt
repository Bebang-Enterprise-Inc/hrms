CREATE UNIQUE INDEX pos_orders_bill_number_natural_key ON public.pos_orders USING btree (location_id, business_date, bill_number, channel) WHERE ((bill_number IS NOT NULL) AND (is_duplicate = false))
