# S243 Closeout Summary

**Sprint:** Canonical CoA Backfill — 4 BEBANG ENTERPRISE INC. Stores
**Plan version:** v1.1 (8 CRITICAL audit fixes applied 2026-05-09)
**Completed:** 2026-05-09
**Branch:** `s243-canonical-coa-4-stores`
**PR:** #735
**Worktree:** `F:/Dropbox/Projects/BEI-ERP-s243-execute`

---

## Pre-state vs post-state

| Store | Abbr | Pre accounts | Post accounts | Created | Existed |
|---|---|---:|---:|---:|---:|
| ROBINSONS ANTIPOLO - BEBANG ENTERPRISE INC. | ROA | 5 | 8 | 3 | 0 |
| SM MANILA - BEBANG ENTERPRISE INC. | SMM | 5 | 8 | 3 | 0 |
| SM MEGAMALL - BEBANG ENTERPRISE INC. | SMMM | 5 | 8 | 3 | 0 |
| SM SOUTHMALL - BEBANG ENTERPRISE INC. | SMS | 5 | 8 | 3 | 0 |
| **Total** | | **20** | **32** | **12** | **0** |

`coa_complete_count` post-seed: **49 / 49** ✓ (incomplete_stores: 0)

### New accounts created (12)

Per store, 3 new group accounts (BARE-NAME convention: `account_name` BARE, `account_number = NULL`, Title Case):

- `Current Assets - <ABBR>` (Asset, group, parent = `1100000 - ASSETS - <ABBR>` existing target-store root)
- `Stock Assets - <ABBR>` (Asset, group, parent = `Current Assets - <ABBR>` new)
- `Accounts Payable - <ABBR>` (Liability, group, parent = `2104000 - INTERCOMPANY PAYABLES - <ABBR>` existing target-store Liability root)

---

## Requirements Regression Checklist (PASS/FAIL)

### v1.1 audit-fix items

- [x] **A1 (BARE-NAME convention)**: All 12 created accounts have `account_name` BARE (no abbr suffix), `account_number = NULL`, Title Case. Verified by `seed_ledger.json` (every entry has `expected_docname` = `<account_name> - <abbr>` matching `name`); `grep -c "is_group=0" scripts/s243/seed_canonical_coa_for_4_stores.py` = 0; `grep -c "_ensure_account(" scripts/s243/...` = 0 (uses NEW `_ensure_group_account`).
- [x] **A2 (ignore_root_company_validation)**: Seeder set `frappe.local.flags.ignore_root_company_validation = True` before inserts and restored on exit. `grep -c "ignore_root_company_validation"` = 2 in seeder.
- [x] **A3 (account_name shape)**: `coa_gap_analysis.json` has `account_name` BARE (e.g., `"Stock Assets"`, NOT `"Stock Assets - ROA"`). Seeder passed BARE value to `frappe.get_doc({})`; Frappe constructed `name` per `<account_name> - <abbr>`. Verified: every `seed_ledger.json` row's `name` equals `expected_docname`.
- [x] **A4 (outer-savepoint dry-run)**: Phase 2 dry-run report (`seed_dry_run_report.json`) shows `rollback_confirmed: true`, `total_created: 12`, `total_errors: 0`. Post-dry-run probe confirmed all 4 stores back to 5 accounts (matches pre-state exactly).
- [x] **A5 (evidence regen)**: `tmp/s243/probe_phase0_state.py` exists in worktree (NEW, not cited from `tmp/s238/`). `before_state.json` was generated by it.
- [x] **A6 (Phase 4-T3 SUMMARY.md gates)**: This SUMMARY.md exists with Requirements Regression Checklist as a PASS/FAIL table (this very section).
- [x] **A7 (PR description gate)**: PR #735 body = `cat output/s243/SUMMARY.md`; this checklist visible in PR description.
- [x] **A8 (`git add -f output/`)**: Closeout used `git add -f output/s243/` to stage gitignored evidence files.
- [x] **W5 (rebase-before-push)**: Phase 4-T2 ran `git fetch origin --prune && git rebase origin/production` before push.
- [x] **A9 (B8 follow-up recorded)**: See "Follow-up sprint candidates" section below — extending `verify_canonical_structure.py` is recorded as S244+ candidate.

### Original v1 checks

- [x] Canonical preflight passed pre-execute (Phase 0): `ALL CANONICAL`, 49/49 stores, 0 violations
- [x] Canonical preflight passed post-execute (Phase 4-T1): `ALL CANONICAL`, 49/49 stores, 0 violations
- [x] No new canonical violations introduced (post-check matches pre-check baseline)
- [x] Only the 4 named target Companies received account inserts (per `seed_ledger.json` — 12 inserts, 4 stores × 3 groups)
- [x] All inserted accounts have `is_group=1` (no leaf accounts created — those are S238's job)
- [x] All inserted accounts follow BARE-NAME convention (verified A1)
- [x] Pre-existing accounts on the 4 target stores preserved untouched (`1100000 - ASSETS - <ABBR>`, `2104000 - INTERCOMPANY PAYABLES - <ABBR>`, 3 leaf each — same docnames + parents post-execute)
- [x] No accounts created on any of the 45 complete stores
- [x] No accounts created on `BEBANG KITCHEN INC.`, `BEBANG ENTERPRISE INC.`, or any parent legal entity
- [x] `tabCompany`, `tabWarehouse`, `tabCustomer`, `tabSupplier` unchanged
- [x] No SI / PI / GL / SLE mutations
- [x] `coa_complete_count: 49 / 49` post-seed (per `coa_complete_count.json`)
- [x] S238 `_find_parent_group_for(company, "Stock Assets")` returns non-null for all 4 target stores post-seed
- [x] Same for `Accounts Payable` and `Current Assets` parent lookups
- [x] Plan YAML status updated to COMPLETED
- [x] SPRINT_REGISTRY.md S243 row updated to COMPLETED + PR# filled

---

## Iteration finding (Phase 2 dry-run)

First dry-run attempt: parent `Accounts Payable - <ABBR>` to NULL (top-level, sibling to existing `2104000 - INTERCOMPANY PAYABLES - <ABBR>`). Failed with Frappe error: *"[Account, Accounts Payable - <ABBR>]: parent_account"*. Frappe rejects creating a 2nd top-level Liability root when one already exists (each Company has exactly one root per `root_type`).

**Fix:** parent `Accounts Payable - <ABBR>` to existing `2104000 - INTERCOMPANY PAYABLES - <ABBR>`. Taxonomic compromise — AP appears as sub-category of IC PAYABLES on these 4 stores. S238's `_find_parent_group_for` queries by `account_name` (BARE), not parent chain — so deviation does NOT break S238's lookup. Recorded as follow-up (see below).

The first attempt's transaction was rolled back cleanly (savepoint pattern worked); no production state changed during the failed attempt.

---

## Follow-up sprint candidates (out-of-scope for S243, recorded for S244+)

1. **Q1 Input VAT recovery for the 42 historical Submitted BKI SIs (~PHP 51,765 input VAT)** on these 4 stores. Q1 2550Q deadline (2026-04-25) has passed; recoverable via Q2 carry-forward (deadline 2026-07-25). Source: PH Finance audit PH-3.
2. **Extend `scripts/verify_canonical_structure.py` with a CoA-completeness rule** (e.g., `COA_INCOMPLETE_<root>`) so the same skeleton-CoA gap on a future per-store Company creation surfaces at canonical-preflight time. Source: PH-7 / SA-3 / S243 audit B8.
3. **Full canonical CoA harmonization for the 4 stores** (Sales tree, COGS, Expense, Equity hierarchy + replace existing `1100000 - ASSETS` and `2104000 - INTERCOMPANY PAYABLES` non-canonical roots with `Application of Funds (Assets) - <ABBR>` + `Source of Funds (Liabilities) - <ABBR>`; add `Current Liabilities - <ABBR>` intermediate so AP can move out from under IC PAYABLES). S243 only created the 3 group accounts S238 needs; full harmonization is a bigger sprint.

---

## S238 unblock confirmation

S238 Phase 0-T4 CoA-completeness gate is now passable for all 49 stores. The S238 worktree at `F:/Dropbox/Projects/BEI-ERP-s238-execute` is clean and ready. After S243 PR #735 is merged, S238 can resume from Phase 0-T1 in a fresh session.

S238 next-step prerequisites already met:
- `BEI Settings.bki_sales_naming_series` = `BKI-SI-.YYYY.-.#####` (set 2026-05-08 during S238 v2.1 work; persists through S243)
- All 4 target stores now have all 3 parent groups Stock Assets, Accounts Payable, Current Assets resolvable via S238's `_find_parent_group_for`
- Canonical preflight: `ALL CANONICAL` (no new drift introduced by S243)
