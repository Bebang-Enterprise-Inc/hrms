# S258 DEFECTS — running register

Each row = a deviation from the planned state surfaced during execution. Mode classification per plan §Failure Response (A=app, B=script, C=data, D=Sam policy).

## Phase 0 — Boot + Preflight + Audit

| # | Mode | Subtask | Finding | Disposition |
|---|---|---|---|---|
| D0-1 | D (plan-author) | 0.0 verification gate | Plan said `grep -c "^\| COA-175-0" ≥ 23`. Cleanroom `02_LOCKED_DECISIONS.md` contains exactly 20 numbered locks (COA-175-001..020). No 021/022/023 exist in any cleanroom or canonical source. Gate adjusted to `≥20` at Phase 0.0; total reaches `≥27` after Phase 7.1 adds COA-175-024..030. | ACCEPTED. Source truth (cleanroom) governs; gate recount documented in `verify_phase0.py`. |
| D0-2 | D (plan-author) | 0.6 (audit C5 amend) | v1.1 amendment said III gl_entry_count is non-zero (asserted to "correct v1.0 zero-postings holdco assumption"). Live audit shows III gl_entry_count = 0. v1.0 zero-GL assumption was correct; v1.1 over-corrected. Note: III IS a true zero-GL holdco with 338 accounts (highest of three Apex parents). Design Rationale ordering (III→BKI→BEI) remains valid for *combined* reasons: III has zero-GL (low blast radius) + smallest sales sub-tree complexity. | ACCEPTED. Verified live: III=0 GL, BKI=13660 GL, BEI=2031 GL. Phase 3a still runs first per plan ordering. |
| D0-3 | C (data) | 0.5.5 | 2 Companies have `first_provision_done` != 1: `BEBANG FRANCHISE CORP.` and `BEBANG FT INC.` Both are MISSING status (0 accounts). Expected — they haven't been provisioned yet. Per W7 amendment, Phase 2 + 3 scripts that seed these Companies MUST set `frappe.flags.in_migrate = True` in script preamble to prevent the `auto_provision_company` hook re-firing during seeding. | ACCEPTED. Phase 2.4 (B1 BFC seed) + Phase 2.5 (B2 BFT seed) scripts will include the flag. |
| D0-4 | C (data) | 0.6.5 | Abbr inconsistency audit found 0 inconsistencies (SQL fallback used per v1.2 P1-7; cleanroom `find` returned 0 hits as expected). All Company names + abbrs already UPPER. BFI2→BFT is a SEMANTIC rename (Sam directive) not a case-fix; Phase 1.5 handles it independently. `scope_expansion_required = false`. | ACCEPTED. Phase 1.5 scope unchanged. |
| D0-5 | C (data) | 0.8 | Protected surface registry entry for S238 (store-side PI generator in flight) did NOT appear in worktree's SPRINT_REGISTRY.md (origin/production base). Marked REMOVED-STALE per Phase 0.8 mechanic. Two `[VERIFIED]` file-on-disk entries (S253 bki_store_pi_generator.py, S206 labor_allocation.py) confirmed. | ACCEPTED per plan procedure ("Remove dead entries; cite real paths verified by grep + glob"). |

## Phase 1 — Safe Sync

| # | Mode | Subtask | Finding | Disposition |
|---|---|---|---|---|
| D1-1 | D (plan-design vs canonical-rule) | 1.3 A3 | Plan v1.2 P0-3 called for `frappe.delete_doc(force=True, flags.ignore_links=True)` on the legacy Liability ROUND OFF dupes. In practice `delete_doc` cannot delete an Account that has GL Entries (separate validation, NOT bypassed by ignore_links). Canonical model Rule 2 also mandates disable-don't-delete. **Followed Rule 2:** JE-transferred ROBDA's 0.80 PHP balance (`ACC-JV-2026-00014`) then SET disabled=1 on both ROBDA + XMM legacy Liability dupes. GL Entries preserved (audit trail integrity). | ACCEPTED — Rule 2 (canonical SSOT) takes precedence over plan mechanism. Plan should be amended for v1.3. |
| D1-2 | A (Frappe REST API constraint) | 1.3.5 BEI round_off canonical | Plan called for CREATE `Round Off - BEI` Expense + UPDATE tabCompany.round_off_account. CREATE via REST API fails with `ValidationError: Please add the account to root level Company - IRRESISTIBLE INFUSIONS INC.` because BEI is a child Company; ERPNext requires the account to first exist on the root Company (III) and then cascade. The `ignore_root_company_validation` flag is server-side only (not exposable via REST API). | DEFERRED to Phase 3c. Phase 3a's per-Company 5-root tree seed loop (using bench execute with the flag set) will create `Round Off - BEI` natively under canonical `Expense - BEI > Indirect Expenses - BEI`. BEI's current `round_off_account = "Stock Adjustment - Bebang Enterprise Inc."` remains pointing at a 0-GL Expense account (functional, just non-canonical name) until Phase 3c runs. |
| D1-3 | A (same as D1-2) | 1.1 A1 — III | III is the root holdco (is_group=1, parent of 57 children). Creating `Stock In Hand - III` cascades to all 57 children via ERPNext default behavior. One child Company (`STA. LUCIA EAST GRAND MALL - BEBANG SM MARIKINA INC.`) has a parent docname `CURRENT ASSETS` (no `- <ABBR>` suffix on that specific row), so the cascade fails the parent_account check. | DEFERRED to Phase 3a. Phase 3a's per-Company 5-root tree seed loop creates `Asset - III`, then a future seed creates inventory under it via bench execute. 43 of 44 PARTIAL Companies completed in A1; III alone deferred. |
| D1-4 | B (script bug) | A5 first run | `frappe.rename_doc(... ignore_permissions=False)` raises `TypeError: rename_doc() got an unexpected keyword argument 'ignore_permissions'` in this Frappe version. Fixed in retry — removed the kwarg. 2 Cost Centers (`BEBANG FT INC. - BFT`, `Main - BFT`) renamed successfully on the retry. | RESOLVED in same session. |
| D1-5 | A (Frappe REST API constraint) | A3 first run | `frappe.client.submit` endpoint expects FORM-encoded `doc` (full JSON-string), not JSON body. Added `submit_doc()` helper to `_lib.py`. ROBDA JE `ACC-JV-2026-00014` submitted successfully on retry. | RESOLVED in same session. |
